<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | BadmintonStore Admin</title>
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <%- include('partials/sidebar') %>
    
    <main class="admin-main">
        <header class="admin-header">
            <h1>Products</h1>
        </header>
        
        <div class="filters-bar">
            <form action="/admin/products" method="GET" class="filter-form">
                <input type="text" name="search" placeholder="Search by description..." value="<%= search %>">
                <button type="submit" class="btn btn-outline">Search</button>
                <% if (search) { %>
                    <a href="/admin/products" class="btn btn-outline">Clear</a>
                <% } %>
            </form>
        </div>
        
        <p class="results-count"><%= products.length %> product(s) found</p>
        
        <% if (products.length > 0) { %>
            <table class="admin-table">
                <thead>
                    <tr>
                        <th>Media</th>
                        <th>Description</th>
                        <th>Seller</th>
                        <th>Featured</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% products.forEach(product => { %>
                        <tr>
                            <td>
                                <img src="<%= product.images[0] || 'https://placehold.co/50x50/f5f5f5/333333?text=No+Img' %>" 
                                     alt="Product" 
                                     class="product-thumbnail">
                                <span style="font-size: 0.75rem; color: #6b7280; display: block;">
                                    <%= product.images?.length || 0 %> photo(s)<%= product.video ? ', 1 video' : '' %>
                                </span>
                            </td>
                            <td style="max-width: 400px;"><%= product.description ? product.description.substring(0, 150) + (product.description.length > 150 ? '...' : '') : 'No description' %></td>
                            <td><%= product.seller?.storeName || 'Unknown' %></td>
                            <td>
                                <button class="featured-toggle-btn <%= product.featured ? 'is-featured' : '' %>" 
                                        onclick="toggleProductFeatured('<%= product._id %>', this)"
                                        title="<%= product.featured ? 'Remove from featured' : 'Make featured' %>">
                                    <span class="star-icon"><%= product.featured ? '⭐' : '☆' %></span>
                                </button>
                            </td>
                            <td class="actions">
                                <a href="/admin/products/edit/<%= product._id %>" class="btn btn-small">Edit</a>
                                <form action="/admin/products/delete/<%= product._id %>" method="POST" class="inline-form" onsubmit="return confirm('Are you sure you want to delete this product?');">
                                    <button type="submit" class="btn btn-small btn-danger">Delete</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p class="no-data">No products found.</p>
        <% } %>
    </main>
    
    <script>
        async function toggleProductFeatured(productId, btn) {
            try {
                const response = await fetch(`/admin/products/${productId}/toggle-featured`, {
                    method: 'POST',
                    headers: {
                        'Accept': 'application/json',
                        'Content-Type': 'application/json'
                    }
                });
                
                const data = await response.json();
                
                if (data.success) {
                    const starIcon = btn.querySelector('.star-icon');
                    if (data.featured) {
                        btn.classList.add('is-featured');
                        starIcon.textContent = '⭐';
                        btn.title = 'Remove from featured';
                    } else {
                        btn.classList.remove('is-featured');
                        starIcon.textContent = '☆';
                        btn.title = 'Make featured';
                    }
                }
            } catch (error) {
                console.error('Error toggling featured:', error);
                alert('Failed to update featured status');
            }
        }
    </script>
</body>
</html>
