<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | RacketBazaar Admin</title>
    <link rel="stylesheet" href="/css/admin.css">
    <style>
        .status-badge { padding: 0.25rem 0.75rem; border-radius: 999px; font-size: 0.875rem; font-weight: 600; }
        .status-badge.status-pending { background: #fef3c7; color: #b45309; }
        .status-badge.status-active { background: #d1fae5; color: #047857; }
        .status-badge.status-deactivated { background: #fee2e2; color: #b91c1c; }
        .status-badge.status-featured { background: #fef3c7; color: #b45309; }
        .status-badge.status-not-featured { background: #f3f4f6; color: #6b7280; }
        
        .action-card { background: white; border-radius: 12px; padding: 1.5rem; margin-top: 1.5rem; box-shadow: 0 2px 8px rgba(0,0,0,0.08); }
        .action-card h3 { margin-bottom: 1rem; font-size: 1rem; }
        .action-buttons { display: flex; flex-wrap: wrap; gap: 0.75rem; }
        
        .btn-approve { background: #10b981; color: white; border: none; padding: 0.5rem 1.25rem; border-radius: 8px; cursor: pointer; font-weight: 500; }
        .btn-approve:hover { background: #059669; }
        .btn-reject { background: #ef4444; color: white; border: none; padding: 0.5rem 1.25rem; border-radius: 8px; cursor: pointer; font-weight: 500; }
        .btn-reject:hover { background: #dc2626; }
        .btn-deactivate { background: #f59e0b; color: white; border: none; padding: 0.5rem 1.25rem; border-radius: 8px; cursor: pointer; font-weight: 500; }
        .btn-deactivate:hover { background: #d97706; }
        .btn-activate { background: #10b981; color: white; border: none; padding: 0.5rem 1.25rem; border-radius: 8px; cursor: pointer; font-weight: 500; }
        .btn-activate:hover { background: #059669; }
        
        .pending-notice { background: #fef3c7; border: 2px solid #f59e0b; border-radius: 12px; padding: 1.5rem; margin-bottom: 1.5rem; }
        .pending-notice h3 { color: #b45309; margin-bottom: 0.5rem; }
        .pending-notice p { color: #92400e; margin-bottom: 1rem; }
        .pending-notice .action-buttons { margin-top: 1rem; }
        
        .seller-detail-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
        @media (max-width: 768px) { .seller-detail-grid { grid-template-columns: 1fr; } }
    </style>
</head>
<body>
    <%- include('partials/sidebar') %>
    
    <main class="admin-main">
        <header class="admin-header">
            <h1><%= seller.storeName %></h1>
            <a href="/admin/sellers" class="btn btn-outline">← Back to Sellers</a>
        </header>
        
        <!-- Pending Approval Notice -->
        <% if (seller.status === 'pending') { %>
        <div class="pending-notice">
            <h3>⏳ Awaiting Approval</h3>
            <p>This seller registered on <%= new Date(seller.createdAt).toLocaleDateString() %> and is waiting for your approval. Their store and products are currently hidden from the website.</p>
            <div class="action-buttons">
                <form action="/admin/sellers/<%= seller._id %>/approve" method="POST" style="display: inline;">
                    <button type="submit" class="btn-approve">✓ Approve Seller</button>
                </form>
                <form action="/admin/sellers/<%= seller._id %>/reject" method="POST" style="display: inline;" onsubmit="return confirm('Reject and DELETE this seller? This cannot be undone.');">
                    <button type="submit" class="btn-reject">✕ Reject & Delete</button>
                </form>
            </div>
        </div>
        <% } %>
        
        <div class="seller-detail-grid">
            <div class="order-info-card">
                <h3>Seller Information</h3>
                <table class="info-table">
                    <tr>
                        <td>Name:</td>
                        <td><%= seller.name %></td>
                    </tr>
                    <tr>
                        <td>Store:</td>
                        <td><%= seller.storeName %></td>
                    </tr>
                    <tr>
                        <td>Phone:</td>
                        <td><%= seller.phone %></td>
                    </tr>
                    <tr>
                        <td>Email:</td>
                        <td><%= seller.email || 'Not provided' %></td>
                    </tr>
                    <tr>
                        <td>Joined:</td>
                        <td><%= new Date(seller.createdAt).toLocaleDateString() %></td>
                    </tr>
                    <tr>
                        <td>Status:</td>
                        <td>
                            <span class="status-badge status-<%= seller.status %>">
                                <%= seller.status === 'pending' ? '⏳ Pending Approval' : seller.status === 'active' ? '✓ Active' : '✕ Deactivated' %>
                            </span>
                        </td>
                    </tr>
                    <tr>
                        <td>Featured:</td>
                        <td>
                            <span class="status-badge status-<%= seller.featured ? 'featured' : 'not-featured' %>">
                                <%= seller.featured ? '⭐ Featured Seller' : 'Not Featured' %>
                            </span>
                        </td>
                    </tr>
                </table>
            </div>
            
            <div class="order-info-card">
                <h3>Statistics</h3>
                <div class="seller-stats">
                    <div class="mini-stat">
                        <span class="mini-stat-number"><%= stats.totalProducts %></span>
                        <span class="mini-stat-label">Products</span>
                    </div>
                    <div class="mini-stat">
                        <span class="mini-stat-number"><%= stats.totalOrders %></span>
                        <span class="mini-stat-label">Orders</span>
                    </div>
                    <div class="mini-stat">
                        <span class="mini-stat-number">Rs. <%= stats.totalRevenue.toLocaleString() %></span>
                        <span class="mini-stat-label">Revenue</span>
                    </div>
                </div>
                
                <% if (seller.status !== 'pending') { %>
                <p style="margin-top: 1rem; font-size: 0.875rem; color: #6b7280;">
                    <% if (seller.status === 'active') { %>
                        ✓ Store is <strong>visible</strong> on the website
                    <% } else { %>
                        ✕ Store is <strong>hidden</strong> from the website
                    <% } %>
                </p>
                <% } %>
            </div>
        </div>
        
        <!-- Actions Card -->
        <div class="action-card">
            <h3>Actions</h3>
            <div class="action-buttons">
                <form action="/admin/sellers/<%= seller._id %>/toggle-featured" method="POST" style="display: inline;">
                    <button type="submit" class="btn <%= seller.featured ? 'btn-outline' : 'btn-featured' %>">
                        <%= seller.featured ? '☆ Remove Featured' : '⭐ Make Featured' %>
                    </button>
                </form>
                
                <% if (seller.status === 'active') { %>
                <form action="/admin/sellers/<%= seller._id %>/deactivate" method="POST" style="display: inline;">
                    <button type="submit" class="btn-deactivate" onclick="return confirm('Deactivate this seller? Their store will be hidden.')">
                        Deactivate Seller
                    </button>
                </form>
                <% } else if (seller.status === 'deactivated') { %>
                <form action="/admin/sellers/<%= seller._id %>/approve" method="POST" style="display: inline;">
                    <button type="submit" class="btn-activate">
                        Reactivate Seller
                    </button>
                </form>
                <% } %>
                
                <form action="/admin/sellers/<%= seller._id %>/delete" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to DELETE this seller? This cannot be undone.');">
                    <label style="display: inline-flex; align-items: center; gap: 0.5rem; margin-right: 0.5rem;">
                        <input type="checkbox" name="deleteProducts"> Also delete products
                    </label>
                    <button type="submit" class="btn btn-danger">Delete Seller</button>
                </form>
            </div>
        </div>
        
        <!-- Products -->
        <div class="order-items-card" style="margin-top: 1.5rem;">
            <h3>Seller's Products (<%= products.length %>)</h3>
            <% if (seller.status !== 'active') { %>
            <p style="color: #b45309; font-size: 0.875rem; margin-bottom: 1rem;">
                ⚠️ These products are currently hidden from the website because the seller is <%= seller.status %>.
            </p>
            <% } %>
            <% if (products.length > 0) { %>
                <table class="admin-table">
                    <thead>
                        <tr>
                            <th>Image</th>
                            <th>Name</th>
                            <th>Category</th>
                            <th>Price</th>
                            <th>Stock</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% products.forEach(product => { %>
                            <tr>
                                <td>
                                    <img src="<%= product.images[0] || 'https://placehold.co/50x50/f5f5f5/333333?text=No+Img' %>" 
                                         alt="<%= product.name %>" 
                                         class="product-thumbnail">
                                </td>
                                <td><%= product.name %></td>
                                <td><%= product.category %></td>
                                <td>Rs. <%= product.price.toLocaleString() %></td>
                                <td><span class="<%= product.stock < 5 ? 'low-stock' : '' %>"><%= product.stock %></span></td>
                                <td>
                                    <a href="/admin/products/edit/<%= product._id %>" class="btn btn-small">Edit</a>
                                    <form action="/admin/products/delete/<%= product._id %>" method="POST" class="inline-form" onsubmit="return confirm('Delete this product?');">
                                        <button type="submit" class="btn btn-small btn-danger">Delete</button>
                                    </form>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <p class="no-data">This seller has no products.</p>
            <% } %>
        </div>
    </main>
</body>
</html>
