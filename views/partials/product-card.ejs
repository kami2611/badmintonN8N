<div class="product-card">
    <a href="/products/<%= product._id %>" class="product-card-link">
        <div class="product-image">
            <img src="<%= product.images[0] || 'https://placehold.co/400x400/f5f5f5/333333?text=No+Image' %>" alt="<%= product.name %>">
            <% if (product.featured) { %>
                <span class="product-badge">Featured</span>
            <% } %>
        </div>
    </a>
    <div class="product-info">
        <% if (product.brand) { %>
            <span class="product-brand"><%= product.brand %></span>
        <% } %>
        <a href="/products/<%= product._id %>">
            <h3 class="product-name"><%= product.name %></h3>
        </a>
        <div class="product-footer">
            <p class="product-price">$<%= product.price.toFixed(2) %></p>
            <button class="btn-cart add-to-cart" data-id="<%= product._id %>" title="Add to Cart">
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"></circle><circle cx="20" cy="21" r="1"></circle><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path></svg>
            </button>
        </div>
        <% if (product.sellerInfo) { %>
            <a href="/store/<%= product.sellerInfo._id %>" class="product-seller" onclick="event.stopPropagation()">
                <span class="store-icon">🏪</span> <%= product.sellerInfo.storeName %>
            </a>
        <% } %>
    </div>
</div>
